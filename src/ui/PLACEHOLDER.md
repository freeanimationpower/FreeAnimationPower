# PLACEHOLDER — Directorio UI v1 (obsoleto)

La interfaz v1 fue reemplazada completamente por `src/ui_v2/`.
Este directorio se mantiene vacio como marcador de migracion.

Todo el codigo UI activo esta en `src/ui_v2/`.
